version: '1'
title: Clean Energy API
description: Clean Energy backend API Gateway Documentation. All HTTP Errors are default, except for 422, that is used for validation errors and the responses will show the incorrect inputs. Unique attributes are not verified on validation, if you, for example, send a email that is already registered the error will be a generic Bad Request.

servers:
  - url: https://foo.bar
    description: TEMPORARY STAGE - CHANGE THIS URL AND DESCRIPTION

models:
  - ${file(docs/models/Success.yml)}
  - ${file(docs/models/Error.yml)}
  - ${file(docs/models/BadRequest.yml)}
  - ${file(docs/models/CheckEligibilityRequest.yml)}
